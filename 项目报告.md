# 考勤管理系统 — 项目报告

**学号：42411142 姓名：薛磊**

---

## 一、项目介绍

### 1.1 项目背景

在高校教学管理中，课堂考勤是教学秩序管理的重要环节。传统的纸质点名方式效率低下、数据难以统计、容易出错。随着信息化建设的推进，利用计算机技术实现考勤管理的数字化、自动化已成为必然趋势。

### 1.2 项目目标

本项目旨在开发一套基于 B/S 架构的课堂考勤管理系统，实现以下总体目标：

- 学生能够通过 Web 端快速完成课堂签到打卡
- 教师和管理员能够便捷地管理学生、课程、课次、选课等基础数据
- 系统能够根据签到时间自动判定考勤状态（正常/迟到），并提供考勤统计功能
- 支持 Excel 批量导入学生信息，提高数据录入效率
- 基于角色的权限控制（管理员/教师/学生），保障数据安全

### 1.3 项目意义

本系统将传统的线下考勤流程迁移至线上，降低了教师的管理成本，提高了考勤数据的准确性和可追溯性。同时，通过自动化的状态判定和统计功能，为教学管理提供了数据支撑。

---

## 二、功能描述

### 2.1 用户认证与权限控制

系统采用 Spring Security 框架实现用户认证与授权，支持三种角色：

| 角色 | 权限范围 |
|------|----------|
| ADMIN（管理员） | 全部功能：用户管理、学生管理、教室管理、课程管理、课次管理、选课管理、考勤管理 |
| TEACHER（教师） | 学生管理（仅查看+新增）、教室/课程/课次/选课（仅查看）、考勤管理及导出（仅限本人课程） |
| STUDENT（学生） | 打卡签到、查看个人考勤统计 |

- 用户注册时自动分配 STUDENT 角色，同时创建对应的学生档案
- 侧边栏根据角色动态显示可访问的菜单项
- API 层通过 `@PreAuthorize` 注解实现方法级权限拦截
- 密码采用 BCrypt 加密存储
- **修改密码**：学生和教师可在侧边栏底部点击"修改密码"按钮，通过模态框验证旧密码后自助更新密码
- **教师权限限制**：教师对学生管理仅有查看、新增、导入权限（不可编辑或删除），对教室/课程/课次/选课模块仅有查看权限；前端隐藏操作按钮，后端 API 和页面路由双重拦截

### 2.2 用户管理（ADMIN 专属）

管理员可对系统用户进行增删改查操作：

- 支持按用户名、真实姓名、角色筛选
- 分页展示用户列表，密码字段不显示
- 新增用户时可选择角色：管理员、教师、学生
- 创建学生角色用户时自动同步创建学生档案
- 编辑用户时可修改角色，系统自动处理角色变更带来的数据同步（如从教师改为学生时创建学生档案，从学生改为其他角色时清理学生档案）
- 删除学生用户时级联清理学生档案、选课记录、考勤记录

### 2.3 学生管理

支持对学生信息的完整维护：

- 学生列表支持按学号、姓名、性别、注册日期筛选
- 新增/编辑学生基本信息（学号、姓名、性别）
- 新增学生时自动创建登录账号（用户名为学号，默认密码为 swufe）
- 删除学生时级联清理用户账号、选课记录和考勤记录
- **Excel 批量导入**：支持上传 `.xlsx` / `.xls` 格式文件，批量导入学生信息，自动创建对应的登录账号。导入过程提供成功/失败计数和详细错误提示
- 导入文件格式：第一行为标题头（学号、姓名、性别），从第二行开始为数据

### 2.4 教室管理

- 教室列表支持按 ID、名称筛选
- 新增/编辑教室信息：名称、行数、列数、不可坐位置
- 不可坐位置以 `行-列` 格式存储（如 `1-1,1-5`），列表展示时自动解析为"第1行第1列，第1行第5列"的中文格式

### 2.5 课程管理

- 课程列表展示：课程编号、课程名称、教师姓名、教室 ID、上课星期、起止周
- 课程关联教师（从用户表选取 TEACHER 角色）和教室
- 新增/编辑课程表单提供教师和教室下拉选择
- 教师姓名通过数据库 JOIN 查询展示，避免显示原始 ID

### 2.6 课次管理

- 课次是课程的具体上课时间安排，一个课程可以有多个课次
- 支持精确到分钟的上课日期时间设置（`datetime-local` 控件）
- 课程名称通过 JOIN 查询展示
- 支持按课程编号、上课日期筛选
- 课次状态：进行中 / 已结束

### 2.7 选课管理

- 记录学生与课程的选课关系
- 列表同时展示学号、学生姓名、课程名称（均通过 JOIN 查询）
- 新增选课时提供学生和课程下拉选择
- 支持按学号、课程编号筛选

### 2.8 考勤管理（核心功能）

#### 打卡签到

学生登录后进入首页，系统自动加载该学生当日已选课次的签到情况：

- 已签到：显示签到时间和考勤状态标签
- 未签到：根据课次时间分三种情况
  - 课次未开始 → 显示"未开始"标签，不可签到
  - 课次开始后 20 分钟内 → 显示"未签到"标签，可点击签到按钮
  - 超过课次 20 分钟 → 显示"缺勤"标签，签到窗口关闭

点击签到后，系统执行以下逻辑：

1. 检查是否已签到（防止重复）
2. 若当前时间早于课次时间 → 拒绝，提示"签到未开始"
3. 若当前时间超过课次时间 20 分钟 → 拒绝，提示"签到已结束"
4. 签到时间在课次时间 10 分钟以内 → 状态为 **NORMAL（正常）**
5. 签到时间在课次时间 10~20 分钟之间 → 状态为 **LATE（迟到）**

此外，系统会在学生查看今日课次时自动将超 20 分钟未签到的学生置为 ABSENT（缺勤），保证统计数据完整。

#### 考勤状态体系

| 状态 | 含义 | 判定方式 |
|------|------|----------|
| NORMAL | 正常 | 课次开始后 ≤10 分钟签到 |
| LATE | 迟到 | 课次开始后 10~20 分钟签到 |
| EARLY | 早退 | 由教师/管理员手动设置 |
| ABSENT | 缺勤 | 超 20 分钟未签到（自动标记）或无签到记录 |

#### 考勤记录管理

教师和管理员可查看全部考勤记录：

- 列表展示：课程名称、学号、学生姓名、签到时间、状态、IP（通过 JOIN 查询关联名称，不再显示原始 ID）
- 支持按学号、状态、日期范围筛选
- 每行提供状态下拉框，可手动修改考勤状态
- 支持删除考勤记录

#### 按课程查看考勤

管理员和教师可在考勤统计页面通过课程下拉框查看某课程所有选课学生的考勤详情：

- 展示每个学生在各课次的签到时间与状态
- 从未签到过的学生也会列出（签到时间为空、状态为缺勤）
- 支持对已有记录修改状态或删除
- 支持对缺勤学生进行"补签"（新建指定状态的考勤记录）

#### 考勤统计

- 选择学生后展示其考勤统计数据
- 统计维度：总记录数、正常次数、迟到次数、缺勤次数、出勤率
- 出勤率 = (正常 + 迟到 + 早退) / 总记录数 × 100%
- 以进度条可视化展示出勤率
- 学生登录后自动展示本人统计，无需选择

#### CSV 导出

支持将课程考勤记录导出为 CSV 文件：

- 管理员可导出任意课程的考勤记录
- 教师仅可导出自己教授课程的考勤记录（后端权限校验）
- CSV 内容：课程号、课程名称、学号、姓名、签到状态（中文）
- UTF-8 BOM 编码，Excel 打开中文不乱码

---

## 三、数据库设计

### 3.1 E-R 图（文字描述）

系统包含 7 张数据表，实体关系如下：

- **user**（用户）1 : 1 **student**（学生）—— 通过 `user.username = student.student_id` 关联
- **user**（教师）1 : N **course**（课程）—— 通过 `user.id = course.teacher_id` 关联
- **classroom**（教室）1 : N **course**（课程）—— 通过 `classroom.id = course.classroom_id` 关联
- **course**（课程）1 : N **course_session**（课次）
- **course**（课程）1 : N **course_selection**（选课）
- **student**（学生）1 : N **course_selection**（选课）
- **course_session**（课次）1 : N **attendance**（考勤）
- **student**（学生）1 : N **attendance**（考勤）

### 3.2 数据表字段

#### user（用户表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(100) | BCrypt 加密密码 |
| real_name | VARCHAR(50) | 真实姓名 |
| role | VARCHAR(20) | 角色：ADMIN / TEACHER / STUDENT |
| create_time | DATETIME | 创建时间 |

#### student（学生表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| student_id | VARCHAR(20) | 学号，主键，与 user.username 对应 |
| student_name | VARCHAR(50) | 学生姓名 |
| gender | VARCHAR(2) | 性别（男/女） |
| create_time | DATETIME | 创建时间 |

#### classroom（教室表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT | 主键，自增 |
| classroom_name | VARCHAR(50) | 教室名称，唯一 |
| rows | TINYINT | 座位行数 |
| cols | TINYINT | 座位列数 |
| exclude_seats | VARCHAR(200) | 不可坐位置（如 1-1,1-5） |
| create_time | DATETIME | 创建时间 |

#### course（课程表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| course_id | VARCHAR(20) | 课程编号，主键 |
| course_name | VARCHAR(100) | 课程名称 |
| teacher_id | BIGINT | 教师 ID，关联 user.id |
| classroom_id | INT | 教室 ID，关联 classroom.id |
| weekday | TINYINT | 上课星期（1-7） |
| start_week | INT | 起始教学周 |
| end_week | INT | 结束教学周 |
| create_time | DATETIME | 创建时间 |

#### course_session（课次表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| session_id | BIGINT | 主键，自增 |
| course_id | VARCHAR(20) | 课程编号，关联 course.course_id |
| session_date | DATETIME | 上课日期时间（精确到分钟） |
| week_number | INT | 第几教学周 |
| status | TINYINT | 状态：1-进行中，0-已结束 |
| create_time | DATETIME | 创建时间 |

#### course_selection（选课表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| student_id | VARCHAR(20) | 学号，关联 student.student_id |
| course_id | VARCHAR(20) | 课程编号，关联 course.course_id |
| select_time | DATETIME | 选课时间 |

#### attendance（考勤表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| session_id | BIGINT | 课次 ID，关联 course_session.session_id |
| student_id | VARCHAR(20) | 学号，关联 student.student_id |
| check_in_time | DATETIME | 签到时间 |
| seat_row | TINYINT | 座位行号（预留） |
| seat_col | TINYINT | 座位列号（预留） |
| status | VARCHAR(20) | 考勤状态：NORMAL / LATE / EARLY / ABSENT |
| ip | VARCHAR(15) | 签到 IP 地址 |
| create_time | DATETIME | 创建时间 |

---

## 四、创新功能与技术难点

### 4.1 创新功能

**1. 智能考勤状态判定**

区别于传统的仅记录签到时间，系统在签到时会自动获取课次的计划上课时间，计算签到时间与计划时间的差值，自动判定考勤状态。签到分为三个时间窗口：课次开始前不可签到，开始后 10 分钟内为正常，10~20 分钟为迟到，超过 20 分钟签到窗口关闭。同时系统会自动为超 20 分钟未签到的学生创建缺勤记录，保证考勤数据完整。

**2. 按课程维度的考勤总览**

教师和管理员可在考勤统计页面按课程查看所有选课学生的考勤详情。系统将课次、选课、考勤三张表交叉关联，自动展示每个学生每节课次的签到状态。从未签到过的学生也会列出（签到时间空、状态缺勤），并支持通过"补签"为其手动创建考勤记录。

**3. CSV 导出与教师权限控制**

管理员可导出任意课程的考勤记录为 CSV 文件（课程号、课程名称、学号、姓名、签到状态均以中文呈现），教师仅可导出自己教授课程的记录。CSV 采用 UTF-8 BOM 编码确保 Excel 直接打开中文不乱码。教师在导出时经过后端权限校验（非管理员则验证课程 teacher_id 与当前用户 ID 是否一致）。

**4. User-Student 双表自动同步**

系统在架构上将登录账号（user 表）与学生信息（student 表）分离，但通过应用层实现了全场景的自动同步：注册时双写、管理员创建学生时自动创建账号、角色变更时的自动创建或清理、删除时的多表级联。保证了数据一致性，避免了数据孤岛。

**5. Excel 批量导入**

基于 Apache POI 实现的学生信息批量导入功能，支持 `.xlsx` 和 `.xls` 两种格式。导入过程中自动进行数据校验（学号和姓名非空检查、重复检查），并为每位成功导入的学生自动创建登录账号。采用分批提交（每 200 条一批）优化写入性能。

**6. 角色自适应界面与教师权限分级**

前端侧边栏和后端 API 均实现了基于角色的访问控制。学生登录后仅能看到首页打卡、考勤统计和修改密码功能；教师可查看所有管理模块但不可新增/编辑/删除（学生管理除外，保留查看+新增）；管理员拥有全部权限。前端按钮和服务端接口均做双端权限拦截，URL 直接访问也会被拒绝。

**7. 不可坐位置的可视化解析**

教室的不可坐位置以 `行-列` 的紧凑格式存储在数据库中（如 `1-1,1-5,3-2`），在前端列表展示时自动解析为"第1行第1列，第1行第5列，第3行第2列"的可读格式，方便管理员直观理解座位布局。

### 4.2 技术难点

**1. Spring Security 与页面级权限的配合**

Spring Security 的 `@PreAuthorize` 作用于 API 层，但页面路由（PageController）返回的是 Thymeleaf 模板，无法简单地在方法上加注解阻止页面渲染（会导致 403 错误页而非友好提示）。解决方案是在模板中使用 `th:if` 根据角色条件渲染内容，并在 SecurityConfig 中开放页面路由，仅对 API 层做严格拦截。

**2. LocalDateTime 的前后端协调**

课次时间字段从 `VARCHAR` 迁移为 `DATETIME` 类型后，需要协调 Java 端的 `LocalDateTime`、MyBatis 的字段映射、Jackson 的 JSON 序列化/反序列化、以及前端 `datetime-local` 控件的格式（`yyyy-MM-ddTHH:mm` → `yyyy-MM-ddTHH:mm:ss`）。需要注意 `@JsonFormat` 注解的模式必须与前端发送的格式完全一致，否则会导致反序列化失败。

**3. MyBatis JOIN 查询展示关联名称**

课程列表需要显示教师姓名（而非教师 ID）、课次列表需要显示课程名称、选课列表需要显示学生姓名和课程名称。这些都需要在 MyBatis 的 SQL 中使用 LEFT JOIN 并将结果映射到实体类的非持久化字段上。需要注意列别名与 `map-underscore-to-camel-case` 设置的配合。

**4. 双表一致性维护**

User 表和 Student 表在逻辑上是同一实体的两个维度（登录信息 vs. 学生信息），但数据库层面分离。需要在新增、修改、删除等多个操作中维护一致性，包括注册（双写）、管理员新增（交叉创建）、角色变更（创建或清理）、删除（多表级联）等场景。

**5. CSV 文件导出的编码与流管理**

CSV 导出时需要通过 BOM（`﻿`）确保 Excel 正确识别 UTF-8 中文编码。在 Java Servlet 中，若先调用 `response.getOutputStream()` 写 BOM 字节再调用 `response.getWriter()` 写文本内容，会抛出 `IllegalStateException: getOutputStream() has already been called`。解决方案是将 BOM 作为字符 `﻿` 通过 `PrintWriter.print()` 输出，统一使用一个流。

**6. 考勤自动标记缺勤的时机设计**

系统需要在课次开始 20 分钟后自动将未签到学生置为缺勤（创建 ABSENT 记录）。由于无定时任务，采用了"懒加载"策略：学生查看今日课次时触发 per-student 检查，教师查看课程考勤时触发 per-session 批量检查。每次检查前先查询是否已有记录（幂等），避免重复创建。

---

## 五、个人小结

### 5.1 课程学习小结

通过本学期的 JavaEE 开发实践课程，我系统地学习了 Spring Boot 框架的核心组件和 Web 应用开发的完整流程。从最初的 Spring Security 认证配置，到 MyBatis 数据库映射，再到 Thymeleaf 模板引擎和前端 JavaScript 交互，课程内容循序渐进地覆盖了企业级 Java Web 开发的主要技术栈。

在项目实践中，我深刻体会到了"动手做"相比"听课学"的巨大差异。很多在课堂上听起来理所当然的设计（如分层架构、权限控制、数据库设计），在真正编码时会暴露出大量细节问题。例如，最初设计的 User 表和 Student 表分离方案虽在理论上合理，但在实际运行中暴露了诸多数据一致性问题，促使我不断优化和完善。

### 5.2 AI 辅助学习实例与小结

在本项目的开发过程中，我大量使用了 AI 编程助手（Claude Code）辅助开发。以下是一些典型的使用实例和体会：

**实例一：全栈代码生成与查错**

在开发课程管理、课次管理等模块时，我通过自然语言描述需求，AI 帮助生成了 Entity、Mapper、Service、Controller 四层代码以及对应的前端 HTML/JS 文件。更重要的是，AI 在代码审查中发现了多处运行时隐患，例如 `CourseMapper.findById()` 缺失 SQL 映射导致的 `BindingException`、`StudentMapper.xml` 中 `keyProperty="id"` 指向不存在的字段等问题。这些 bug 如果靠手工排查，可能需要大量调试时间。

**实例二：数据一致性问题的系统性分析**

User 表和 Student 表分离带来的数据一致性问题是我在项目中期才意识到的。AI 帮助我系统性地追踪了所有涉及两张表的操作路径（注册、创建、删除、修改、批量导入等共 12 个场景），逐一分析了每个场景下的数据不一致风险，并给出了分优先级的修复方案。

**实例三：数据库类型迁移**

在将 `session_date` 字段从 `VARCHAR` 改为 `DATETIME` 时，AI 帮助我追踪了所有受影响的代码（Entity 类型变更、SQL 日期匹配改为 `DATE()` 函数、前端控件类型变更、JSON 格式协调等），一次性完成了全链路适配，避免了遗漏导致的运行时错误。

**实例四：功能迭代中的需求澄清与全链路实现**

在后期优化阶段（签到规则调整、按课程查看考勤、CSV 导出、教师权限限制、修改密码），AI 在动手前主动就"班级"概念（确认为课程）、签到时间窗口划分、"补签"机制、CSV 编码问题等细节与我确认，然后一次性完成从 Mapper → Service → Controller → 前端 HTML/JS 的全链路修改。在 CSV 导出遇到 `getOutputStream() has already been called` 错误时，AI 通过分析响应体内容和日志快速定位了流混用问题并修复。

**AI 使用小结**

AI 编程助手在本项目中扮演了"高效协作者"的角色。它极大加速了重复性代码的编写（CRUD 模板、前端表格页面等），在代码审查和数据一致性分析方面展现了出色的全局视野。但同时，我也认识到 AI 并非万能——它有时会忽略特定项目上下文的约束（如 Thymeleaf 的嵌套表达式语法错误），最终仍需开发者具备足够的技术判断力来验证和修正。

总的来说，AI 辅助编程让我将更多精力投入到系统设计和问题分析层面，而不是耗费在重复的编码工作上。这种"人定方向、AI 执行、人做审核"的协作模式，是我在本课程中收获的最有价值的实践经验之一。

---

*项目源码地址：[attendance-system](https://github.com/symarxmj/attendance-system)*
