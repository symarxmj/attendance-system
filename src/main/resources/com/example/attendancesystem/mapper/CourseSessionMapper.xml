<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.attendancesystem.mapper.CourseSessionMapper">

    <select id="findByCourseIdsAndDate" resultType="com.example.attendancesystem.entity.CourseSession">
        select * from course_session
        where course_id in
        <foreach collection="courseIds" item="courseId" open="(" separator="," close=")">
            #{courseId}
        </foreach>
        and DATE(session_date) = #{sessionDate}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="sessionId">
        insert into course_session(course_id, session_date, week_number, status)
        values(#{courseId}, #{sessionDate}, #{weekNumber}, #{status})
    </insert>

    <delete id="delete">
        delete from course_session where session_id = #{sessionId}
    </delete>

    <update id="update">
        update course_session
        <set>
            <if test="courseId != null and courseId != ''">course_id = #{courseId},</if>
            <if test="sessionDate != null and sessionDate != ''">session_date = #{sessionDate},</if>
            <if test="weekNumber != null">week_number = #{weekNumber},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        where session_id = #{sessionId}
    </update>

    <select id="findById" resultType="com.example.attendancesystem.entity.CourseSession">
        select * from course_session where session_id = #{sessionId}
    </select>

    <select id="list" resultType="com.example.attendancesystem.entity.CourseSession">
        select cs.*, c.course_name
        from course_session cs
        left join course c on cs.course_id = c.course_id
        <where>
            <if test="courseId != null and courseId != ''">
                and cs.course_id = #{courseId}
            </if>
            <if test="sessionDate != null and sessionDate != ''">
                and DATE(cs.session_date) = #{sessionDate}
            </if>
        </where>
        order by cs.session_date desc, cs.session_id desc
    </select>

</mapper>
