<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.attendancesystem.dao.StudentDao">
    <insert id="insertStudent" parameterType="com.example.attendancesystem.entity.Student" useGeneratedKeys="true" keyProperty="id">
        insert into student(student_id, student_name, gender, create_time)
        values(#{studentId}, #{studentName}, #{gender}, #{createTime})
    </insert>
    
    <select id="findStudentById" resultType="com.example.attendancesystem.entity.Student">
        select student_id, student_name, gender, create_time
        from student
        where student_id = #{studentId}
    </select>

    <delete id="delete">
        delete from student
        where student_id = #{studentId}
    </delete>
    
    <update id="updateStudent">
        update student
        set student_name = #{studentName}, gender = #{gender}
        where student_id = #{studentId}
    </update>
</mapper>