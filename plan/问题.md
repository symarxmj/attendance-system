1. 当用户登录成功后，welcome页面的内容应该居中显示，可以稍稍放大一点
2. 对于签到的功能，检测签到的时间和课次的时间，在10min中之内签到才算签到成功，否则算失败
3. 对于签到的功能，数据库中存储的状态为：NORMAL（正常）/LATE（迟到）/EARLY（早退）/ABSENT（缺勤），按照这个形式来改一下
4. 对于签到的功能，对于教师和管理员应该添加相应的编辑功能，只可以更改status签到的状态，可以将NORMAL改为LATE什么的